class officer{
    constructor(firstName, lastName, salary){
        this.firstName = firstName;
        this.lastName = lastName;
        this.salary = salary;
    }
};

const nurNabi = new officer("NUR", "NABI", 30000); // to add data inside a class like forma.
console.log(nurNabi);

const hasanAli = new officer("HASAN", "ALI", 40000); //
console.log(hasanAli);